# Firebase Configuration & Rules
This directory contains configuration files and client SDK code for Firebase services (Firestore and Authentication).

## Files:
- `firebaseConfig.js`: Initializes and exports Firestore database (`db`) and Authentication (`auth`) services.
